version: '2.0'
services:
  web:
    container_name: arm-instabot
    build: ./Dockerfile-arm
    ports:
      - "8080:80"
    depends_on:
      - db
#      - redis
    volumes:
      - ./:/var/www/html
#      - .env.example:/var/www/html/.env
    env_file:
      - .env.example
  db:
#    container_name: arm-instabot-db
    image: hypriot/rpi-mysql
    env_file:
      - .env.example
#  redis:
#    image: redis:4.0.5-alpine
#  phpmyadmin:
#    image: phpmyadmin/phpmyadmin
#    ports:
#      - "8583:80"
#    depends_on:
#      - db
#  front:
#    image: mohsentm/csms-front:latest
#    volumes:
#      - .env.example:/usr/src/app/.env
#    ports:
#      - "3000:3000"
